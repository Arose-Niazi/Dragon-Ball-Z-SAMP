/*  DBZ / stuff / server_vars.inc  —  server-wide state
 *  (Populated in later milestones: DB handle, ban cache, event state, etc.)
 */
#if defined _dbz_server_vars_included
    #endinput
#endif
#define _dbz_server_vars_included

// Reserved for global server state. Intentionally empty at Milestone 1.
